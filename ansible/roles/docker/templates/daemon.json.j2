{
  {% if docker_registry_mirror | length %}"registry-mirrors": ["{{ docker_registry_mirror }}"],{% endif %}
  "data-root": "{{ docker_data_root_path }}",
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
