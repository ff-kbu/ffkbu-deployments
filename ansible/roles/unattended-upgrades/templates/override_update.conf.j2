#jinja2: lstrip_blocks: "True", trim_blocks: "True"
# {{ ansible_managed }}
[Timer]
# Override needs to reset OnCalendar first by setting an empty var
# otherwise they will add up with the default 6:00 timer in
# /lib/systemd/system/apt-daily.timer
OnCalendar=
OnCalendar={{ unattended_daily_update_cron_time }}
RandomizedDelaySec={{ unattended_daily_update_cron_delay_sec }}
Persistent=false